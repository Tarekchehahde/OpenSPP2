<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Tree View -->
    <record id="view_spp_dci_data_source_tree" model="ir.ui.view">
        <field name="name">spp.dci.data.source.tree</field>
        <field name="model">spp.dci.data.source</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
                <field name="code" />
                <field
                    name="state"
                    decoration-success="state == 'active'"
                    decoration-danger="state == 'error'"
                    decoration-warning="state == 'testing'"
                    decoration-muted="state == 'draft'"
                    widget="badge"
                />
                <field name="registry_type" />
                <field name="base_url" widget="url" />
                <field name="auth_type" />
                <field name="active" widget="boolean_toggle" />
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_spp_dci_data_source_form" model="ir.ui.view">
        <field name="name">spp.dci.data.source.form</field>
        <field name="model">spp.dci.data.source</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,testing,active,error"
                    />
                    <button
                        name="action_test_connection"
                        string="Test Connection"
                        type="object"
                        class="btn-primary"
                    />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Data Source Name" />
                        </h1>
                    </div>

                    <!-- Basic Information -->
                    <group string="Basic Information">
                        <group>
                            <field
                                name="code"
                                help="Unique identifier for this data source"
                            />
                            <field
                                name="registry_type"
                                help="Type of registry: DR Client or Indicator"
                            />
                        </group>
                        <group>
                            <field
                                name="active"
                                widget="boolean_toggle"
                                help="Inactive sources will not be used for data retrieval"
                            />
                        </group>
                    </group>

                    <!-- Connection Settings -->
                    <group string="Connection Settings">
                        <group>
                            <field
                                name="base_url"
                                widget="url"
                                help="Base URL of the DCI registry API"
                            />
                            <field
                                name="verify_ssl"
                                help="Verify SSL certificates for HTTPS connections"
                            />
                        </group>
                        <group>
                            <field
                                name="timeout"
                                help="Connection timeout in seconds"
                            />
                        </group>
                    </group>

                    <!-- DCI Protocol Configuration -->
                    <group string="DCI Protocol Configuration">
                        <group>
                            <field
                                name="our_sender_id"
                                help="Our sender ID for DCI messaging protocol"
                            />
                            <field
                                name="our_callback_uri"
                                widget="url"
                                help="URI where we receive callbacks from the remote system"
                            />
                        </group>
                        <group>
                            <field
                                name="receiver_id"
                                help="Receiver ID of the remote DCI system"
                            />
                        </group>
                    </group>

                    <!-- Authentication -->
                    <group string="Authentication">
                        <group>
                            <field
                                name="auth_type"
                                help="Authentication method to use with this data source"
                            />
                            <field
                                name="bearer_token"
                                widget="password"
                                invisible="auth_type != 'bearer'"
                                help="Static Bearer token sent as the Authorization header"
                            />
                            <field
                                name="oauth2_token_url"
                                widget="url"
                                invisible="auth_type != 'oauth2'"
                                help="OAuth2 token endpoint URL"
                            />
                            <field
                                name="oauth2_client_id"
                                invisible="auth_type != 'oauth2'"
                                help="OAuth2 client ID"
                            />
                            <field
                                name="oauth2_client_secret_display"
                                widget="password"
                                invisible="auth_type != 'oauth2'"
                                help="OAuth2 client secret (write-only: value is masked after saving)"
                            />
                            <field
                                name="oauth2_scope"
                                invisible="auth_type != 'oauth2'"
                                help="OAuth2 scope (optional)"
                            />
                            <field
                                name="oauth2_credential_location"
                                invisible="auth_type != 'oauth2'"
                                help="RFC 6749 standard uses request body. OpenCRVS requires query parameters."
                            />
                        </group>
                    </group>

                    <!-- Message Signing -->
                    <group string="Message Signing">
                        <group>
                            <field
                                name="signing_key_id"
                                help="Key ID used for signing DCI messages"
                            />
                        </group>
                    </group>

                    <!-- API Endpoints -->
                    <group
                        string="API Endpoints (Optional Overrides)"
                        name="api_endpoints"
                    >
                        <group>
                            <field
                                name="search_endpoint"
                                help="Custom search endpoint (if different from base_url/search)"
                            />
                            <field
                                name="subscribe_endpoint"
                                help="Custom subscribe endpoint (if different from base_url/subscribe)"
                            />
                        </group>
                        <group>
                            <field
                                name="auth_endpoint"
                                help="Custom auth endpoint (if different from base_url/auth)"
                            />
                        </group>
                    </group>

                    <notebook>
                        <page string="Connection Status" name="connection_status">
                            <group>
                                <group>
                                    <field
                                        name="last_test_date"
                                        readonly="1"
                                        help="Last time connection was tested"
                                    />
                                </group>
                                <group>
                                    <field
                                        name="last_error"
                                        readonly="1"
                                        help="Last error message from connection test"
                                    />
                                </group>
                            </group>
                        </page>
                        <page string="Notes" name="notes">
                            <field
                                name="notes"
                                placeholder="Additional notes about this data source..."
                            />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_spp_dci_data_source_search" model="ir.ui.view">
        <field name="name">spp.dci.data.source.search</field>
        <field name="model">spp.dci.data.source</field>
        <field name="arch" type="xml">
            <search string="DCI Data Sources">
                <field name="name" />
                <field name="code" />
                <field name="base_url" />
                <filter
                    string="Active"
                    name="filter_active"
                    domain="[('active', '=', True)]"
                />
                <filter
                    string="Inactive"
                    name="filter_inactive"
                    domain="[('active', '=', False)]"
                />
                <filter
                    string="Draft"
                    name="filter_state_draft"
                    domain="[('state', '=', 'draft')]"
                />
                <filter
                    string="Testing"
                    name="filter_state_testing"
                    domain="[('state', '=', 'testing')]"
                />
                <filter
                    string="Active Connection"
                    name="filter_state_active"
                    domain="[('state', '=', 'active')]"
                />
                <filter
                    string="Error"
                    name="filter_state_error"
                    domain="[('state', '=', 'error')]"
                />
                <filter
                    string="DR Client"
                    name="filter_registry_dr_client"
                    domain="[('registry_type', '=', 'dr_client')]"
                />
                <filter
                    string="Indicator Registry"
                    name="filter_registry_indicator"
                    domain="[('registry_type', '=', 'indicator')]"
                />
                <filter
                    string="OAuth2"
                    name="filter_auth_oauth2"
                    domain="[('auth_type', '=', 'oauth2')]"
                />
                <filter
                    string="API Key"
                    name="filter_auth_api_key"
                    domain="[('auth_type', '=', 'api_key')]"
                />
                <filter
                    string="No Auth"
                    name="filter_auth_none"
                    domain="[('auth_type', '=', 'none')]"
                />
                <group>
                    <filter
                        string="State"
                        name="group_by_state"
                        context="{'group_by': 'state'}"
                    />
                    <filter
                        string="Registry Type"
                        name="group_by_registry_type"
                        context="{'group_by': 'registry_type'}"
                    />
                    <filter
                        string="Authentication Type"
                        name="group_by_auth_type"
                        context="{'group_by': 'auth_type'}"
                    />
                    <filter
                        string="Active Status"
                        name="group_by_active"
                        context="{'group_by': 'active'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_spp_dci_data_source" model="ir.actions.act_window">
        <field name="name">Data Sources</field>
        <field name="res_model">spp.dci.data.source</field>
        <field name="view_mode">list,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Data Source
            </p>
            <p>
                Configure external data sources for DCI integration.
                Data sources define endpoints for DR Client and Indicator registries.
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <!-- Root DCI Menu under Settings > Technical -->
    <menuitem
        id="menu_spp_dci_root"
        name="DCI"
        parent="base.menu_custom"
        sequence="50"
    />

    <!-- Configuration Submenu -->
    <menuitem
        id="menu_spp_dci_config"
        name="Configuration"
        parent="menu_spp_dci_root"
        sequence="10"
    />

    <!-- Activity Logs Submenu -->
    <menuitem
        id="menu_spp_dci_activity"
        name="Activity Logs"
        parent="menu_spp_dci_root"
        sequence="20"
    />

    <!-- Data Sources under Configuration -->
    <menuitem
        id="menu_spp_dci_data_source"
        name="Data Sources"
        parent="menu_spp_dci_config"
        action="action_spp_dci_data_source"
        sequence="10"
    />
</odoo>
